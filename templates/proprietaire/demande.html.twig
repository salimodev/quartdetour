<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Nouvelle demande de prix</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #fff;
      color: #333;
      margin: 0;
      padding: 40px;
    }

    h1 {
      text-align: center;
      font-size: 28px;
      margin-bottom: 10px;
    }

    p.description {
      text-align: center;
      color: #666;
      margin-bottom: 30px;
    }

    .form-container {
      max-width: 700px;
      margin: 0 auto;
      background: #fff;
      border: 1px solid #eee;
      border-radius: 12px;
      padding: 25px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.05);
    }

    h3 {
      margin-top: 25px;
      font-size: 18px;
    }

    label {
      display: block;
      margin-top: 10px;
      font-weight: 600;
    }

    input[type="text"], input[type="email"], input[type="number"], textarea, select {
      width: 100%;
      padding: 10px;
      border: 1px solid #ddd;
      border-radius: 8px;
      margin-top: 6px;
      font-size: 15px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
    }

    table th, table td {
      text-align: left;
      padding: 8px;
    }

    table input, table select {
      width: 100%;
    }

    .btn-add, .btn-photo {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border: 1px solid #ff4500;
      color: #ff4500;
      background: #fff;
      border-radius: 8px;
      padding: 8px 15px;
      cursor: pointer;
      font-weight: 600;
      margin-top: 10px;
      transition: background 0.3s;
    }

    .btn-add:hover, .btn-photo:hover {
      background: #ffe9e0;
    }

    .btn-record {
      display: flex;
      align-items: center;
      justify-content: center;
      border: 1px solid #ff4500;
      color: #ff4500;
      background: #fff;
      border-radius: 8px;
      padding: 10px;
      font-weight: 600;
      margin-top: 10px;
      cursor: pointer;
    }

    .btn-record:hover {
      background: #ffe9e0;
    }

    .checkbox-group {
      margin-top: 15px;
    }

    .checkbox-group label {
      display: block;
      margin-top: 8px;
    }

    .btn-submit {
      display: block;
      width: 100%;
      background: #ff5a1f;
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 14px;
      font-size: 16px;
      font-weight: 600;
      margin-top: 25px;
      cursor: pointer;
      transition: background 0.3s;
    }

    .btn-submit:hover {
      background: #ff732f;
    }

    .row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }

    .col {
      flex: 1;
      min-width: 48%;
    }

    .link {
      color: #ff5a1f;
      text-decoration: none;
      font-weight: 600;
    }

    .link:hover {
      text-decoration: underline;
    }

    @media (max-width: 600px) {
      .col {
        min-width: 100%;
      }
    }
  </style>
</head>
<body>

  <!-- Informations véhicule -->
<div class="section">
  <h3>Informations véhicule</h3>

  <label for="marque">Marque du véhicule</label>
  <input type="text" id="marque" name="marque" placeholder="Ex : Peugeot, Renault..." />

  <label for="numero_chassis">Numéro de châssis</label>
  <input type="text" id="numero_chassis" name="numero_chassis" placeholder="Ex : VF3XXXXXXXXXXXXXX" />

  <label for="type_carburant">Type de carburant</label>
  <select id="type_carburant" name="type_carburant">
    <option value="">-- Sélectionner --</option>
    <option value="diesel">Diesel</option>
    <option value="essence">Essence</option>
    <option value="hybride">Hybride</option>
    <option value="electrique">Électrique</option>
  </select>

  <label for="etat_moteur">État du moteur</label>
  <select id="etat_moteur" name="etat_moteur">
    <option value="">-- Sélectionner --</option>
    <option value="bon">Bon état</option>
    <option value="moyen">Moyen</option>
    <option value="a_reparer">À réparer</option>
  </select>

  <label for="carte_grise">Carte grise (photo)</label>
  <input type="file" id="carte_grise" name="carte_grise" accept="image/*" />

</div>

<style>
  .section {
    background: #fff;
    padding: 20px;
    border-radius: 10px;
    margin-bottom: 25px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.08);
  }

  .section h3 {
    color: #ff4d00;
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 15px;
    border-left: 4px solid #ff4d00;
    padding-left: 8px;
  }

  label {
    display: block;
    font-weight: 600;
    margin-top: 10px;
    margin-bottom: 5px;
    color: #333;
  }

  input[type="text"], select, input[type="file"] {
    width: 100%;
    padding: 10px;
    border-radius: 8px;
    border: 1px solid #ccc;
    font-size: 15px;
    transition: border-color 0.3s;
  }

  input:focus, select:focus {
    border-color: #ff4d00;
    outline: none;
  }
</style>

</body>
</html>
